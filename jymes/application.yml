eureka:
  instance:
    hostname: 10.30.2.133
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://admin:${REGISTRY_SERVER_PASSWORD:fd2018}@10.30.2.133:8761/eureka/

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000

ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000

spring:
  rabbitmq:
    host: 10.30.2.133
  sleuth:
    sampler:
      percentage: 1
    integration:
      enabled: false
    scheduled:
      skip-pattern: "^org.*HystrixStreamTask$"

authserver:
    hostname: 10.30.2.133
    port: 8767
    contextPath: /uaa

security:
  oauth2:
    resource:
      user-info-uri: http://${authserver.hostname}:${authserver.port}${authserver.contextPath}/current
